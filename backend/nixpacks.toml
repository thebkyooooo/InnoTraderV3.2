# Nixpacks 빌드 설정 — JDK 21 고정 + Spring Boot bootJar 빌드
# (실행 명령은 railway.json의 deploy.startCommand가 담당)

[phases.setup]
nixPkgs = ["jdk21"]

[phases.build]
cmds = [
  "chmod +x ./gradlew",
  "./gradlew bootJar -x test --no-daemon"
]
